---
import Section from "../../ui/Section.astro";

interface Props {
  heading: string;
  paragraphs: string[];
  capabilityLabels: string[];
}

const { heading, paragraphs, capabilityLabels } = Astro.props;
---
<Section id="co-delame" variant="light" spacing="md" borderedTop={true}>
  <div class="grid gap-10 lg:grid-cols-[1fr_0.9fr] lg:gap-14">
    <div class="space-y-6">
      <p class="font-heading text-xs uppercase tracking-[0.16em] text-primary">Co delame</p>
      <h2 class="max-w-prose">{heading}</h2>
      <div class="text-prose space-y-4">
        {paragraphs.map((paragraph) => <p>{paragraph}</p>)}
      </div>
    </div>

    <aside aria-label="Rozsah kompetenci" class="rounded-card border border-border bg-bg-alt p-8">
      <p class="mb-5 font-heading text-xs uppercase tracking-[0.16em] text-text-muted">
        Rozsah kompetenci
      </p>
      <ul class="grid gap-3 sm:grid-cols-2">
        {
          capabilityLabels.map((label) => (
            <li class="rounded-full border border-border bg-white px-4 py-3 text-sm font-medium text-text-main">
              {label}
            </li>
          ))
        }
      </ul>
    </aside>
  </div>
</Section>

